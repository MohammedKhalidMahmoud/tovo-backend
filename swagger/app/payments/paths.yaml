/payments/me:
  get:
    tags: [Payments]
    summary: List my completed payments (customer)
    security: [{ BearerAuth: [] }]
    parameters:
      - name: page
        in: query
        schema: { type: integer, default: 1 }
      - name: limit
        in: query
        schema: { type: integer, default: 20 }
    responses:
      '200':
        description: Payments list
        content:
          application/json:
            schema:
              allOf:
                - $ref: '#/components/schemas/SuccessResponse'
                - type: object
                  properties:
                    data:
                      type: array
                      items: { $ref: '#/components/schemas/Payment' }
      '401': { description: Unauthorized }
      '403': { description: Forbidden }

/payments/{id}:
  get:
    tags: [Payments]
    summary: Get payment by ID (customer or admin)
    security: [{ BearerAuth: [] }]
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    responses:
      '200':
        description: Payment details
        content:
          application/json:
            schema:
              allOf:
                - $ref: '#/components/schemas/SuccessResponse'
                - type: object
                  properties:
                    data: { $ref: '#/components/schemas/Payment' }
      '401': { description: Unauthorized }
      '403': { description: Forbidden }
      '404': { description: Payment not found }

  get:
    tags: [Payments]
    summary: List all completed payments (admin)
    security: [{ BearerAuth: [] }]
    parameters:
      - name: page
        in: query
        schema: { type: integer, default: 1 }
      - name: limit
        in: query
        schema: { type: integer, default: 20 }
      - name: status
        in: query
        schema: { type: string }
      - name: userId
        in: query
        schema: { type: string, format: uuid }
      - name: driverId
        in: query
        schema: { type: string, format: uuid }
      - name: paymentType
        in: query
        schema: { type: string, enum: [cash] }
      - name: dateFrom
        in: query
        schema: { type: string, format: date-time }
      - name: dateTo
        in: query
        schema: { type: string, format: date-time }
    responses:
      '200':
        description: Payments list
        content:
          application/json:
            schema:
              allOf:
                - $ref: '#/components/schemas/SuccessResponse'
                - type: object
                  properties:
                    data:
                      type: array
                      items: { $ref: '#/components/schemas/Payment' }
      '401': { description: Unauthorized }
      '403': { description: Forbidden }
